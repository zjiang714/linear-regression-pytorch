线性回归：

​	线性回归其实是一个预测、优化问题。现行模型可以看作是单层神经网络

​	1、线性回归是对n维输入的加权，外加偏差

​	2、使用平方损失来衡量预测值和真实值的差异

​	3、线性回归有显示解

​	4、线性回归可以看作是单层神经网络



基础优化算法：

​	1、梯度下降通过不断沿着反梯度方向更新参数求解

​	2、小批量随机梯度下降是深度学习默认的求解算法

​	3、两个重要的超参数是批量大小



线性回归从0开始实现：

​	1、

| `reshape(-1,1)` | 变成列向量 |
| --------------- | ---------- |
|                 |            |

| `reshape(1,-1)` | 变成行向量 |
| --------------- | ---------- |
|                 |            |

| `reshape(-1)` | 变成一维 |
| ------------- | -------- |
|               |          |

| `reshape(2,3)` | 固定形状 |
| -------------- | -------- |
|                |          |



2、`torch.tensor` 是 PyTorch 中 **创建张量（Tensor）的函数**









### **问**：features, labels = synthetic_data(true_w, true_b, 1000)这一行代码生成了1000份样本对吗？但是为什么要根据真实的true_w和true_b来生成样本呢？ 

**答**：**如果没有 `true_w` 和 `true_b` 作为“造数”的依据，我们就没有“标准答案”来评价模型学得好不好了。**

我们可以从以下三个维度来理解为什么要这么做：

#### 1. 为了模拟“真实的自然规律”

在现实世界中，很多现象背后都有隐藏的规律（虽然我们看不见）。

- **例子**：比如“房价”可能真的和“面积”有某种线性关系。
- **代码模拟**：我们用 `true_w` 和 `true_b` 扮演这个**“隐藏的自然规律”**。
- **生成样本**：通过它们生成 1000 份样本，就相当于我们在现实中收集到了 1000 套房子的成交记录。

------

#### 2. 为了给模型一个“老师”

机器学习需要一个“反馈机制”。

- 如果我们随机生成 `labels`（答案），那数据就是乱码，模型什么也学不到。
- 我们根据 `true_w` 生成 `labels`，就相当于给模型请了一位**永远正确、逻辑一致的老师**。
- 模型在训练时，会不断对比自己的预测值和这些由“真理”生成的 `labels`，从而知道自己该往哪个方向修正。

------

#### 3. 为了验证我们的算法“灵不灵”

这是最关键的一点。在科研和学习阶段，我们需要**验证**。

- **已知点**：我们心里知道标准答案是 `[2.0, -3.4]`。
- **过程**：我们把模型（学生）关在小黑屋里，只给它看 1000 份样本（题目和答案），不告诉它公式。
- **测试**：等它训练完了，我们看看它计算出来的 `w` 是多少。
- **结论**：如果它算出来的结果非常接近 `[2.0, -3.4]`，那就证明我们的**模型结构**和**训练算法（SGD）**是有效的！

------

#### 总结

使用 `true_w` 和 `true_b` 生成数据，就像是在实验室里做一个**“受控实验”**。





问：数据能是随机的吗？

回答：不能，机器学习的前提是必须要有相关性，**间必须存在某种函数关系 y=f(x)**

如果数据是随机的，当你运行那段循环代码时，**损失函数（Loss）降不下来**、梯度（Grad）乱跳、权重（w）变成噪音



**问**：那就是与真实的true_w和true_b数据相差很大，但是依旧可以用features, labels =  synthetic_data(true_w, true_b, 1000)来生成1000个样本，但是需要修改y1 = y +  torch.normal(0, 0.01, y.shape)中的0.01那个噪声点？还能用这行代码生成吗？

答**可以。** 你只需要调整代码中那个代表“人间烟火”的**噪声项**即可。

在你现在的代码中： `y1 = y + torch.normal(0, 0.01, y.shape)`

- **目前的 `0.01`**：代表这是一个非常精准、听话的世界。数据点几乎全都排在 `true_w` 定义的那条直线上。
- **模拟“跳跃很大”**：你只需要把 `0.01` 改成 `5.0` 甚至 `10.0`。
  - 此时，虽然背景里依然有 `true_w` 这个规律，但每个数据点都会被随机“踢”得很远。
  - 反映在图表上，就是散点变得极其凌乱，不再是一条明显的直线。

------

#### 3. 当数据跳跃很大时，训练会发生什么？

这取决于“跳跃”的本质：

| 情况                   | 现象                               | 结果                                                         |
| ---------------------- | ---------------------------------- | ------------------------------------------------------------ |
| **规律还在，噪音变大** | 数据虽然乱，但整体趋势还是向上的。 | **模型依然能学到真理**，但需要更多的数据（比如从 1000 份增加到 10000 份）才能抵消噪音。 |
| **规律不在，全是乱跳** | 数据之间完全没有逻辑关联。         | **模型会彻底失败**。`loss` 会一直很高，无法收敛。            |

导出到 Google 表格

------

#### 4. 为什么要这样写代码？

这就是为什么我们在 `synthetic_data` 函数里要特意加上 `torch.normal(0, 0.01, y.shape)`：

- **如果不加**：模型会学得太轻松，甚至会产生“高分低能”的幻觉（过拟合）。
- **如果加一点**：模型就必须学会透过表面的“乱跳”，去抓取背后的“本质规律”。这才是深度学习真正的功用——**在嘈杂的数据中寻找真理。**



## **损失函数**

损失函数就是衡量预测值与真实值之间的误差

L1 Loss:  ![image-20260318193059890](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260318193059890.png)|真实值 y — 预测值 y’|  绝对值，L1 拥有对噪声的抵抗力。

L2 Loss    均方损失：![image-20260318192351450](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260318192351450.png)，真实值 y — 预测值 y’，L2 在精细调节时有优势。

Huber's  Robust Loss：![image-20260318193252207](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260318193252207.png)





常用函数记忆：

```
torch.normal:创建一个全新的张量
data.normal_：这个方法用于将张量中的元素修改为服从**正态分布（高斯分布）**的随机数
data.fill_：它把张量里的所有元素都变成同一个指定的数值
nn.Flatten()：是一个极其常用的“塑形”层。它的作用非常纯粹：**把一个多维的张量“压扁”成一个二维张量**
torch.tensor([2, -3.4])：手动创建一个包含两个特定数值的 PyTorch 张量：（Tensor）tensor([ 2.0000, -3.4000])
data.TensorDataset：把多个张量（Tensors）对齐并合并成一个数据集对象
d2l.load_data_fashion_mnist：这一步完成了下载、转换 (Transform)：把图片从原始格式转成 PyTorch 的 Tensor、封装 (Dataset)、小批量化 (DataLoader)
relu:激活函数，有负数变 0，正数不变的特性，为模型注入“非线性”能力，没有relu则模型只能表达线性关系
torch.ones_like： 是 PyTorch 中一个非常实用的“克隆”函数。它的核心作用是：根据一个已有的张量（Tensor），创建一个形状（Shape）完全相同、但数值全为 1 的新张量
optimizer.zero_grad()：清空之前的梯度
loss.backward()：反向传播
optimizer.step()：更新参数（根据算出的梯度，挪动一下权重）
| ----------- | ----------- |
| AMP         | 加速“前向+反向计算” |
| fused AdamW | 加速“参数更新”    |

```



层的定义：在深度学习中，“层”的定义通常由**带参数的权重层**决定，有几组w、b，则这个模型就有多少层，但是此net中每一层必须使用激活函数，目的是让这个net具有非线性计算能力，如果没有定义激活函数，则在数学中即使使用了多层，仍然会坍塌为一个单层模型。

# 感知机：

是一个二分类模型，它的求解算法等价于使用批量大小为1的剃度下降。

激活函数：引入非线性激活函数是为了让神经网络能够处理非线性问题，增加模型的表达能力，使模型可以学习到更复杂的函数关系，避免模型成为简单的线性模型组合而无法有效处理世纪中的非线性数据。



多层感知机函数公式：![image-20260323201309561](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260323201309561.png)



sigmoid激活函数：![image-20260323201751050](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260323201751050.png)

tanh激活函数：![image-20260323201905259](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260323201905259.png)



#### 最常用的是relu激活函数：![image-20260323202058000](/home/zjianglinux/snap/typora/112/.config/Typora/typora-user-images/image-20260323202058000.png)





总结：1、多层感知机使用隐藏层和激活函数来得到非线性模型

​	    2、常使用的激活函数是sigmoid、Tanh、Relu

​	    3、使用softmax来处理多类分类

​            4、超参数为隐藏层数，和各个隐藏层的大小





## 过拟合与欠拟合

#### 过拟合：

​	过拟合就是模型“学得太多了”，它不仅学会了数据中的规律，还把噪声和细节当成规律记住了。这就好比一个学生在考试前死记硬背了答案，但稍微换一道题就不会了

#### 欠拟合：

​	欠拟合就是模型“学得太少了”。它只掌握了最基本的规律，无法捕获数据中的复杂模式。这就像一个学生只学到了皮毛，考试的时候连最简单的题都答不对



处理过拟合：

​	权重衰退：

```
l = loss(net(X), y) + lambd * l2_penalty(w)  # 权重衰退的直接用法，lambd就是权重的具体值，
                                             # 通过修改 lambd 的大小来告诉模型：“我有多在意权重的大小”
                                             # w 的更新是一个“自驱动”的过程
```

​	暂退法：

​		nn.Dropout(dropout2),   # 暂退法的直接用法，定义好dropout2的具体参数

  

​         抗过拟合的方法有2种，

​	第1种是通过权重衰退的方式（在损失函数里加“罚款”，让权重尽量变小）来使模型保持简单，

​	第2种是通过暂退法通过改变网络结构（减少神经元间的协同效应）来保持模型简单。



​	总结：权重衰退通过L2正则项使得模型参数不会过大，从而控制模型复杂度



### 重要算法：

前向传播算法：徒弟射出一支箭、徒弟观察环境（输入），经过大脑思考（计算权重），最后松开弦，箭飞向了靶子。

​	这个过程包括了：输入、权重分配、激活函数

反向传播算法：师父告诉他错在哪、师父站在靶子旁边，大喊一声：**“刚才那箭偏左了！是因为你左手抬得太高了！”**

optimizer.zero_grad()：清空之前的梯度
loss.backward()：反向传播
optimizer.step()：更新参数（根据算出的梯度，挪动一下权重）

​	这个过程包括了：计算误差、责任分配、链式法则

梯度算法：反向传播只是“发现错误并分配责任”，而真正让徒弟做出改变的是**梯度下降**、学习率就是徒弟调整的幅度。

可以用一张图来总结这几个算法：

![image-20260429160402556](/home/zjianglinux/snap/typora/115/.config/Typora/typora-user-images/image-20260429160402556.png)



层：   

![image-20260429184635303](/home/zjianglinux/snap/typora/115/.config/Typora/typora-user-images/image-20260429184635303.png)

块：	

![image-20260429184656806](/home/zjianglinux/snap/typora/115/.config/Typora/typora-user-images/image-20260429184656806.png)									

​	**这种“块”思维的强大之处在于：** 当你设计一个非常复杂的网络（比如有 100 层）时，你不需要写 100 行 `nn.Linear`，你只需要定义一个好用的“块”，然后像复印一样通过循环把它们叠起来即可。

块和层的理解：块就是一个带有记忆的公共函数，前向传播函数放在这个块里面，每一层或者每一个其他的函数在调用这个块的时候，会自动向前传递out 



​	**延后初始化的思维**是：**“我现在不知道输入是多少，等数据真正流过来的时候，你自己看着办吧！”**

以前的写法是：

self.net = nn.Sequential(
    nn.Linear(20, 64),
    nn.Linear(64, 128), # 你得保证这里的 64 和上面对得上
    nn.Linear(128, 1)   # 你得保证这里的 128 和上面对得上
)

引入延后初始化思想后的写法是：

self.net = nn.Sequential(
    nn.LazyLinear(64),  # 输入是多少？不知道，等数据来了再说
    nn.LazyLinear(128), # 输入是多少？自动对齐上一层的 64
    nn.LazyLinear(1)
)

### 读取

读取张量：

x = torch.arange(4)

保存张量：torch.save(x, 'x-file')

读回张量：x2 = torch.load('x-file')
print(x2) # 输出 tensor([0, 1, 2, 3])



读取参数：

--- 保存模型参数 ---

假设你有一个训练好的网络 net

torch.save(net.state_dict(), 'mlp.params')

--- 加载模型参数 ---

1. 先声明一个一模一样的模型骨架

clone_net = MLP() 
2. 把保存的参数“灌”进去

clone_net.load_state_dict(torch.load('mlp.params'))















